import UIKit

struct HeaderItem {
    let match:String
    let increment:CGFloat
    
    init(match:String, increment:CGFloat) {
        self.match = match
        self.increment = increment
    }
}
